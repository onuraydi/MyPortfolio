@model GetPortfolioBlogByPortfolioBlogIdDto

<link href="~/css/quill.css" rel="stylesheet" />

<style>
    /* Yalnızca kod bloklarının görünümünü değiştirmek için */
    .ql-syntax {
        background-color: #2e3440;
        color: #d8dee9;
        padding: 12px;
        border-radius: 6px;
        font-family: monospace;
        font-size: 14px;
    }
</style>

<div>
    <h2 class="mb-3">@Model.Title</h2>
    <div id="blog-content" class="content">@Html.Raw(Model.Content)</div>
        
    
    <div class="tag-widget post-tag-container mb-5 mt-5">
        <div class="tagcloud">
            <a href="#" class="tag-cloud-link">Life</a>
            <a href="#" class="tag-cloud-link">Sport</a>
            <a href="#" class="tag-cloud-link">Tech</a>
            <a href="#" class="tag-cloud-link">Travel</a>
        </div>
    </div>
</div>

@*@await Html.PartialAsync("~/Views/Shared/Components/_BlogAllCommentsContentBlogUILayoutComponentPartial/Default.cshtml",new {id = ViewBag.Blogid})*@